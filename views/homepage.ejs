<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>File Upload</title>

  <!-- Vanta.js + THREE.js -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r121/three.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.waves.min.js"></script>

  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      height: 100vh;
      overflow: hidden;
    }

    #vanta-bg {
      width: 100%;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .upload-container {
      background-color: white;
      padding: 30px 40px;
      border-radius: 30px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      text-align: center;
      width: 350px;
      z-index: 2;
    }

    h1 {
      margin-bottom: 20px;
      color: #333;
    }

    input[type="file"] {
      padding: 10px;
      border: 2px dashed #6c63ff;
      border-radius: 10px;
      margin-bottom: 20px;
      width: 100%;
      transition: border-color 0.3s;
    }

    input[type="file"]:hover {
      border-color: #00b894;
    }

    button {
      background-color: #6c63ff;
      color: white;
      padding: 10px 25px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #574b90;
    }

    .all-files-btn {
      display: inline-block;
      margin-top: 15px;
      padding: 10px 24px;
      background: linear-gradient(to right, #ff6a00, #ee0979);
      color: white;
      border-radius: 10px;
      text-decoration: none;
      font-weight: bold;
      transition: background 0.3s;
    }

    .all-files-btn:hover {
      background: linear-gradient(to right, #e65c00, #d7137f);
    }
  </style>
</head>

<body>
  <div id="vanta-bg">
    <div class="upload-container">
      <h1>📁 Upload Your File</h1>
      <form action="/upload" method="POST" enctype="multipart/form-data">
        <input type="file" name="myfile" required />
        <br><br>
        <button type="submit">Upload</button>
      </form>
      <a href="/files" class="all-files-btn">📁 View All Uploaded Files</a>
    </div>
  </div>

  <script>
    VANTA.WAVES({
      el: "#vanta-bg",
      mouseControls: true,
      touchControls: true,
      minHeight: 200.00,
      minWidth: 200.00,
      scale: 1.0,
      scaleMobile: 1.0,
      color: 0x6c63ff,
      shininess: 50.00,
      waveHeight: 20.00,
      waveSpeed: 0.5,
      zoom: 0.9
    });
  </script>
</body>
</html>
